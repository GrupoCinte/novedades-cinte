# Login va por POST /api/login (backend + Cognito); el JWT de app se guarda en localStorage (cinteAuth).
# No hace falta VITE_COGNITO_* en el bundle: no exponer pool/client en el front salvo un flujo Amplify explícito.

# Proxy de Vite → backend (mismo host/puerto que `PORT` en el .env de la raíz del repo; por defecto 3005).
# Si el backend corre en otro puerto, descomenta y ajusta:
# VITE_API_PROXY_TARGET=http://localhost:3005
# Puerto del dev server de Vite (por defecto 5175; si está ocupado, Vite usará el siguiente, p. ej. 5176).
# VITE_DEV_SERVER_PORT=5175

# Túnel Cloudflare (quick tunnel) en desarrollo: evita HMR roto (wss) y puerto distinto al de cloudflared.
# 1) Backend en 3005, `npm run dev` aquí en 5175. 2) En otra terminal: `npm run tunnel:quick` (requiere `cloudflared` en PATH).
# VITE_TUNNEL_DEV=true

# Opcional: enmascarar email/tel/salario en UI de contratación (CandidateModal.jsx)
# VITE_MASK_SENSITIVE_UI=true

# El módulo Contratación usa rutas /api/contratacion/* y el Bearer del JWT.
# No uses VITE_* para API keys del monitor; el WebSocket usa ticket vía GET /api/contratacion/ws-token.
